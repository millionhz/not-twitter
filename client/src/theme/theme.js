import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#181818',
    },
    secondary: {
      main: '#FB2576',
    },
  },
});

export default theme;
